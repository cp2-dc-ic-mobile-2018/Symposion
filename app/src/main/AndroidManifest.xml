<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.g12.cp2.sympsion">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".gerarQr">
            <intent-filter>
                <action android:name="br.g12.cp2.sympsion.GERARQR"/>
            </intent-filter>
        </activity>
        <activity android:name=".cadastrar">
            <intent-filter>
                <action android:name="br.g12.cp2.sympsion.CADASTRAR"/>
                <!--<action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>-->
            </intent-filter>
        </activity>
        <activity android:name=".cadastrado">
            <intent-filter>
                <action android:name="br.g12.cp2.sympsion.CADASTRADO"/>
            </intent-filter>
        </activity>
    </application>

</manifest>